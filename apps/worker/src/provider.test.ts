import {afterEach,describe,expect,it,vi} from "vitest";import {FakeProvider,OpenAiCompatibleProvider,ProviderError} from "./provider.js";const metrics:any={classification:{typeCode:"unclassified",dimensions:[]},sample:{matchCount:5,queues:[{queueId:420,count:5}],from:"2026-01-01T00:00:00Z",to:"2026-01-02T00:00:00Z"}},skill:any={version:"1",contentHash:"sha256:x",instructions:"trusted instructions"};afterEach(()=>vi.unstubAllGlobals());describe("providers",()=>{it("returns deterministic fake data",async()=>{const p=new FakeProvider();expect(await p.analyze(metrics,skill,new AbortController().signal)).toEqual(await p.analyze(metrics,skill,new AbortController().signal))});it("sends full strict schema and skill instructions",async()=>{const fetch=vi.fn().mockResolvedValue(new Response(JSON.stringify({choices:[{message:{content:"{}"}}]}),{status:200}));vi.stubGlobal("fetch",fetch);await new OpenAiCompatibleProvider("https://provider.example","model","test-key").analyze(metrics,skill,new AbortController().signal);const body=JSON.parse(fetch.mock.calls[0][1].body);expect(body.messages[0].content).toBe(skill.instructions);expect(body.response_format.json_schema.schema.properties.sample.properties.matchCount.minimum).toBe(5);expect(body.response_format.json_schema.schema.additionalProperties).toBe(false)});it.each([[429,"MODEL_RATE_LIMITED",true],[500,"MODEL_TEMPORARILY_UNAVAILABLE",true],[401,"MODEL_AUTH_FAILED",false]])("classifies HTTP %i",async(status,code,retryable)=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue(new Response("",{status:status as number})));await expect(new OpenAiCompatibleProvider("https://x","m","k").analyze(metrics,skill,new AbortController().signal)).rejects.toMatchObject({code,retryable})});it("classifies malformed bodies",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue(new Response("{}",{status:200})));await expect(new OpenAiCompatibleProvider("https://x","m","k").analyze(metrics,skill,new AbortController().signal)).rejects.toBeInstanceOf(ProviderError)})});
